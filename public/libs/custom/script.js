tinymce.init({
	selector : 'textarea.mce'
});

/**
 * Pops up a confirm dialog box.
 */
// $(function() {
// 	$(".btn-delete").click(function(event) {
// 		if (confirm("Are you sure you want to perform deletion? It can't be undone.")) {
// 			$('form.form-delete').submit();
// 		} else {
// 			event.preventDefault();
// 		}
// 	});
// });